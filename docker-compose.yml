# Local Postgres for development. `docker compose up -d` then `npm run db:migrate`.
services:
  db:
    image: postgres:16
    container_name: trustsvan-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: trustsvan
      POSTGRES_PASSWORD: trustsvan
      POSTGRES_DB: trustsvan
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
