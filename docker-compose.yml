# Local Postgres for development. `docker compose up -d` then `npm run db:migrate`.
services:
  db:
    image: postgres:16
    container_name: trusvan-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: trusvan
      POSTGRES_PASSWORD: trusvan
      POSTGRES_DB: trusvan
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
