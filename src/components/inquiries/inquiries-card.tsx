// CONTRACT STUB — owned by the Inquiries agent; replace the body, keep the signature.
/** Trader dashboard card: pending inquiries + recent activity. */
export async function InquiriesCard(props: { userId: string }) {
  void props;
  return null;
}
