// CONTRACT STUB — owned by the Social agent; replace the body, keep the signature.
/** A trader's own non-community posts, newest first, for the public profile's Posts tab. */
export async function ProfilePosts(props: { authorUserId: string; viewerId: string | null }) {
  void props;
  return <p className="text-sm text-zinc-500">No posts yet.</p>;
}
